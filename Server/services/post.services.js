const express = require("express");
const router = express.Router();
const db = require("../../../../Connection/dbConnection");
import { addProductValidation } from '../validations/post.validation.js';


export const addProductsService = async () => {
    
};